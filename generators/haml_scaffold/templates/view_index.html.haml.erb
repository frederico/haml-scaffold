%h2 Listar <%= plural_name %>
- if !@<%= plural_name %>.empty?
  .<%= plural_name %>
    = render :partial => "<%= singular_name %>", :collection => @<%= plural_name %>
  = will_paginate(@<%= plural_name %>)
- else
  %p <%= plural_name %> n‹o cadastrados(as).
= link_to 'Novo(a) <%= singular_name %>', new_<%= singular_name %>_path